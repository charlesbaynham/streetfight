# A6 — Appeals (R8): the player-side appeal, the budget, and the contested queue

Fixture: game `94e4c363-3754-475f-ad4c-f495aca00c5f`, teams `A6-Red` / `A6-Blue`,
players `A6-Alice` (shooter), `A6-Dave` (shooter), `A6-Bob`, `A6-Carol`.
Shots `A1`–`A9`, `D1`–`D6` (ids in `$S/a6/state.json`). All verdicts in this
report were **entered by hand by the admin** — CharlesBot was never involved, so
nothing here depends on the stub vision model. All screenshots 390x844.

---

## Appealing a resolved shot as either shooter or target (R8), including seeing the appeal budget run out.

**Verdict:** WORKS WITH CAVEATS

**Evidence:**

- `$S/shots/a6_01_alice_hud.png` — the HUD carries **"Appeals: 3"** permanently,
  above "My shots >>", so the count is in front of the player before they spend
  one, not only inside the confirmation.
- `$S/shots/a6_03_alice_A3_detail_top.png` — shooter's detail view of a shot
  ruled Missed, amber **Appeal** button present.
- `$S/shots/a6_04_alice_A3_confirm.png` — the reason radios ("It actually hit" /
  "It hit someone else"), "Are you sure? You have 3 of 3 appeals left." and
  "Successful appeals are refunded." The confirm button is disabled until a
  reason is picked (verified: `isDisabled() === true`).
- `$S/shots/a6_06_alice_A3_after_appeal.png` — status flips to ⚖️ **Under
  appeal** over the old verdict, "You appealed: It actually hit", and the HUD
  drops to **Appeals: 2** live over SSE without a reload.
- `$S/shots/a6_08_bob_A1_detail.png`, `a6_09_bob_A1_confirm.png`,
  `a6_10_bob_A1_after.png` — the **target** side, with its own four reasons
  ("It missed me" / "That wasn't me" / "That's not a player" / "I was already
  out"). Bob has fired nothing, so his "My shots" list is entirely
  received shots, which is the half R8 built.
- Reasons land in the right columns. `admin_get_shot_appeal` after both:
  `A3 {"shooter_appeal_reason":"actually_hit","target_appeal_reason":null}`,
  `A1 {"shooter_appeal_reason":null,"target_appeal_reason":"missed"}`.
- **Budget.** `A6-Dave` spent 3 → 2 → 1 → 0 on D1/D2/D3; the fourth
  (`appeal_shot` on D4) returned `400 "You have no appeals left"`.
  `$S/shots/a6_14_alice_A5_no_appeals_left.png` and
  `a6_15_dave_D4_no_appeals_left.png` — the button stays on screen, greyed,
  reading **"No appeals left"** rather than vanishing.
  `admin_give_appeals?num=1` put Dave back to 1 and his open phone changed
  "No appeals left" → "Appeal" live (`a6_16_dave_after_give_appeals.png`); it
  is the only way back, and `num=-1` / `num=-5` floor at 0 rather than going
  negative.
- **Refusals, all clean 400/404 with the same wording the button uses**
  (`backend/user_interface.py:163` `appeal_refusal` is the single source):
  - not adjudicated (A4): `400 "This shot hasn't been adjudicated yet"`, and
    the detail view shows **no** Appeal button at all
    (`a6_11_alice_A4_unreviewed_detail.png`).
  - refunded shot (D5): `400 "There's no verdict on this shot to appeal"`,
    no button (`a6_36_dave_refunded_shot_no_appeal.png`).
  - same party twice: `400 "You've already appealed this shot"`.
  - after the referee has ruled: `400 "The referee has already ruled on an
    appeal against this shot"`, and the button is gone
    (`a6_24_alice_A3_upheld.png`).
  - a third party (Carol on A1) and a non-party (Bob on A6/A7): `404 ... not
    found`, which is the right posture — it does not confirm the id exists.
  - bogus reason: `400 "'because_i_say_so' is not a reason to appeal"`.
- **Both parties on the same shot (A5):** Alice appealed as shooter, Bob then
  saw "⚖️ Under appeal" *and still had his own Appeal button*
  (`a6_26_bob_A5_already_appealed_by_shooter.png`), appealed as target, and both
  reasons were stored side by side
  (`{"shooter_appeal_reason":"wrong_target","target_appeal_reason":"missed"}`).
  `appealed_at` stayed at the **first** complaint, which is what keeps the
  contested ordering honest. When A5 was later overturned both were refunded
  (Alice 0→1, Bob 2→3), as R8 specifies.
- **The refund inference — the subtle bit — is right in every direction I could
  construct:**

  | shot | ruling before → after | appeal_state | budget |
  | --- | --- | --- | --- |
  | A2 | miss → **bystander** | `rejected` | Alice 0 → 0 (no refund) ✓ |
  | A6 | bystander → **miss** | `rejected` | Alice 1 → 1 (no refund) ✓ |
  | D2 | miss → miss | `rejected` | Dave 1 → 1 ✓ |
  | A3 | miss → **hit on Bob** | `upheld` | Alice 0 → 1 (refunded) ✓ |
  | A1 | hit on Bob → **hit on Carol** | `upheld` | Bob 1 → 2 ✓ |
  | A5 | hit on Bob → miss (both appealed) | `upheld` | Alice +1 **and** Bob +1 ✓ |
  | D1 | miss → **Refund** | `upheld` | Dave 0 → 1 ✓ |

  The miss↔bystander pair is the one that is easy to get backwards and it is
  **not** backwards: both directions reject.
  (`backend/admin_interface.py:129` `_same_appeal_outcome`.)
- **Both parties are told.** Alice's ticker after the rulings carried
  `["user","Your appeal was rejected - the referee agreed with the call", <A2 id>]`
  and `["user","Your appeal was upheld - your appeal has been refunded", <A3 id>]`,
  plus the public `["public","The referee overturned CharlesBot: A6-Alice's shot
  is now ruled a hit", <A3 id>]`. The loser is told privately; only the upheld
  case is public. `$S/shots/a6_23_alice_A2_rejected.png` (red ⚖️ "Appeal
  rejected" over the new verdict) and `a6_24_alice_A3_upheld.png` (green
  "Appeal upheld").
- **The scroll fix (0cce40e) is real and necessary.** With my 3:4 test photo the
  Appeal button just fits. With a **9:16 photo — the shape a phone actually
  produces** — it does not: `scrollHeight 717 > clientHeight 675`, the button's
  bottom is below the box, and the bobbing chevron appears
  (`a6_30_bob_A8_tall_photo_fold.png`). Tapping the chevron scrolls it fully
  into view (`a6_31_bob_A8_after_chevron.png`). Without that hint the button
  would read as absent on every real photo.

### Finding: an upheld target appeal erases the shot from the winner's own history
**Severity:** minor
**What happens:** `_record_adjudication` clears `Shot.target_user_id` whenever the
new ruling is not a hit, and `get_shots_received` filters on that column. So the
moment a target's appeal succeeds, the shot leaves their "My shots" list and
`user_shot_image` 404s for them — verified: after A1, A5 and A8 were overturned,
Bob's `user_shots_received` held only A3 (the shot an appeal moved *onto* him),
and `GET user_shot_image?shot_id=<A8>` returned **404**. The private ticker line
"Your appeal was upheld" still carries that shot id and is tappable
(`TickerView.js:63` → `openShotHistory(shotId)`), but the shot is no longer in
the store, so `selectedShot` is null and the popup opens on the **whole list**
instead of the shot. Screenshot `a6_34_bob_tap_upheld_ticker.png`.
**How to reproduce:** admin rules a shot a hit on Bob → Bob appeals "It missed me"
→ admin rules Missed → on Bob's phone tap "Your appeal was upheld" in the ticker.
**Where:** `backend/admin_interface.py:1186` (clears `target_user_id`),
`backend/user_interface.py:578` `get_shots_received`,
`react-ui/src/ShotHistory.js:485` (`selectedShot` lookup).
**Why it matters for the 19th:** the one moment a player most wants to look at the
photo again — "I won, show me" — is the one moment the app cannot show it to
them, and the tap that R8 built specifically for this lands nowhere.

### Finding: HP is not restored when an appeal is upheld, and nothing reminds the admin
**Severity:** major
**What happens:** exactly what R8 decided ("HP is never auto-unwound... the admin
repairs HP and ammo by hand"), but there is no affordance for the second half.
Verified: Alice on Pewster (1 damage) hit Bob, Bob 10 → **9 HP**; Bob appealed;
admin re-ruled Missed; appeal upheld and the appeal refunded — Bob still on
**9 HP** (`a6_33_bob_hp_after_upheld.png`). The contested view has no HP control
and no prompt; the admin has to remember, leave the queue, find the roster and
fix it. If the shot was a knockout the player is still out.
**How to reproduce:** as above with any weapon that does damage.
**Where:** `backend/admin_interface.py:1192` `_settle_appeal` docstring;
`react-ui/src/ShotQueue.js:407` `AppealDetails` (no HP affordance).
**Why it matters for the 19th:** an upheld appeal that leaves the player exactly as
wounded as before is not a correction from the player's point of view, and this
is the busiest, most distracted moment the admin will have all night.

### Finding: the backend accepts an appeal reason belonging to the other party
**Severity:** minor
**What happens:** `appeal_shot` validates only `reason in APPEAL_REASONS`, not
`reason` against the appellant's role. A shooter can lodge `missed`,
`not_a_player` or `already_out` on their own shot — verified twice, `200
{"appealed":true}`, and it spent a real appeal from the budget. The admin then
sees the raw enum, because `ShotQueue.js`'s `APPEAL_REASON_LABELS.shooter` has no
entry for those keys and falls through to the raw value:
`A6-Dave (shooter): "already_out"` — screenshot `a6_37_admin_cross_party_reason.png`.
**How to reproduce:** `POST /api/appeal_shot?shot_id=<your own shot>&reason=already_out`.
**Where:** `backend/user_interface.py:633` (reason check),
`backend/user_interface.py:163` `appeal_refusal` (role check exists, but only for
"target may only appeal a hit"), `react-ui/src/ShotQueue.js:27` and `:447`.
**Why it matters for the 19th:** not reachable from the app's own UI, so it is only
a hazard if somebody pokes the API — but the failure mode is an unreadable
grievance in front of the admin mid-adjudication, and the fix is one membership
test next to the one already there.

### Finding: a player newly convicted by someone else's upheld appeal has no recourse
**Severity:** question
**What happens:** A1 was ruled a hit on Bob; Bob appealed; the admin re-ruled it a
hit on **Carol**. Carol got the private line "You were hit by A6-Alice!" with the
shot id and the shot now sits in her received list — but `appeal_state` is
`upheld`, so `appeal_refusal` returns "The referee has already ruled on an appeal
against this shot" and her Appeal button never appears. The same happens on A3,
where Alice's appeal turned a miss into a hit on Bob. Verified in
`user_shots_received`: `{"appeal_state":"upheld","can_appeal":false}`.
**How to reproduce:** rule a shot a hit on X, have X appeal, re-rule it a hit on Y,
then look at Y's phone.
**Where:** `backend/user_interface.py:175`, `backend/admin_interface.py:1225`.
**Why it matters for the 19th:** R8's own error table says the wrong-target case is
one of the core things appeals exist to fix, and the fix creates a fresh victim
who is told they were hit and has no way to say otherwise. "Resolutions are
terminal" was a deliberate decision, so this may be intended — but the person it
binds never got a turn, which is different from the shooter/target pair the
scheme reasons about. Worth a deliberate yes/no before the night.

### Finding: the public overturn line misattributes the call, and says nothing when the hit only moves
**Severity:** minor
**What happens:** `APPEAL_UPHELD` reads "The referee overturned CharlesBot:
{user}'s shot is now ruled {result}". Two problems seen in my ticker dumps:
(1) CharlesBot was never involved in any of these shots — a human admin made both
the original and the new call — yet it is named as the party overturned. With
`ai_auto_actions_enabled` off (the default, and the plan for the 19th) this is
true of *every* overturn. (2) When A1 moved from a hit on Bob to a hit on Carol,
the line read "…A6-Alice's shot is now ruled **a hit**" — it was already ruled a
hit; `_APPEAL_RESULT_WORDS` names the verdict, never the target, so the
wrong-target correction announces nothing. ("miss → Refund" prints "now ruled
**unreadable**", which is at least deliberate.)
**How to reproduce:** re-rule any contested hit onto a different player and read
the public ticker.
**Where:** `backend/ticker_message_dispatcher.py:127`,
`backend/admin_interface.py:136` `_APPEAL_RESULT_WORDS`,
`backend/admin_interface.py:1250`.
**Why it matters for the 19th:** the public line is, by R8's own words, "the
correction's social event". Announcing a correction that names no change, and
blaming a bot that had no part in it, spends that moment on nothing.

### Finding: the confirmation's scroll chevron sits on top of the appeal count
**Severity:** cosmetic
**What happens:** on the confirm step the popup overflows, so `Popup`'s floating
chevron appears — centred, directly over "Are you sure? You have 3 of 3 appeals
left.", hiding two words of it (`a6_04_alice_A3_confirm.png`).
**Where:** `react-ui/src/Popup.js:90` `scrollHint`.
**Why it matters for the 19th:** it obscures precisely the sentence that makes the
spend informed, on the screen where the spend happens.

---

## The contested-shots list an appeal reopens (R8), separate from the main queue.

**Verdict:** WORKS

**Evidence:**

- `$S/shots/a6_13_admin_queue_contested.png` — `/admin/shots` has a
  Queue / **Contested** radio pair; Contested shows "Shot 1 of 4", an amber
  **"Contested - awaiting your ruling"** panel quoting each appellant with their
  role, the standing verdict and the appeal time, and the normal
  Hit/Missed/Bystander/Refund controls underneath. `a6_28_admin_A5_both_appeals.png`
  shows the two-appellant case rendering both lines
  (`A6-Alice (shooter): "it hit someone else"` / `A6-Bob (target): "it missed me"`).
- **It is a different endpoint, not a filter.** `admin_get_contested_shots_info`
  vs `admin_get_shots_info`. With four of my shots contested,
  `admin_get_shots_info` (unchecked) contained exactly **one** of my shots — A4,
  the one nobody has adjudicated — and none of the contested ones. The
  "Show adjudicated shots" toggle is hidden on the Contested tab.
- **Ordered oldest complaint first, and demonstrably not by shot age.** I appealed
  in a deliberately different order from creation. Creation order `A1, A2, A3, A6`;
  appeal order `A3, A1, A2, A6`; `admin_get_contested_shots_info` returned
  **`A3, A1, A2, A6`**. A5, appealed much later than it was fired, was appended
  last. When Bob appealed A5 second, `appealed_at` did **not** move, so the shot
  keeps its place by the first complaint.
- **A contested shot cannot re-enter the auto-action drain.** Structurally
  guaranteed rather than observed: the shot stays `checked=True`
  (`backend/admin_interface.py:1180`) and the drain only ever reads
  `get_queue_head`, which is `filter_by(checked=False)`
  (`backend/admin_interface.py:212`, `backend/shot_auto_actions.py:96`). The
  observable consequence — contested shots absent from the unchecked queue — I
  did verify. I did **not** run the drain itself: the stub vision model is being
  steered by another agent and I was asked not to touch it.
- Settled shots leave the contested list immediately (A2 dropped out the moment
  it was re-ruled), and re-ruling a contested shot succeeds exactly once —
  `_record_adjudication` allows the re-rule only while `appeal_state == "open"`.

### Finding: nothing anywhere tells the admin an appeal is waiting
**Severity:** minor
**What happens:** the nav badge reads "Shot queue (31)" — that is the **unchecked**
queue only, and contested shots are checked, so the count never moves when an
appeal is lodged. The "Contested" radio carries no count either. An admin working
the Queue tab has no signal at all; they have to speculatively switch tabs.
Confirmed on screen: with 4 contested shots the badge counted only unchecked ones
(`a6_13_admin_queue_contested.png`, `a6_37_admin_cross_party_reason.png`).
**Where:** `react-ui/src/AdminMode.js` (nav count), `react-ui/src/ShotQueue.js:785`
(the Contested radio).
**Why it matters for the 19th:** R8's whole premise is that the admin stops
draining every shot and handles only the contested ones. If appeals are invisible
until you go looking, an appeal can sit unanswered all night while the admin
believes the queue is clear — and a player who has spent a scarce appeal is
waiting on it.

### Finding: `admin_give_appeals` announces the raw number, including negatives
**Severity:** cosmetic
**What happens:** taking appeals away sends the player "You were given **-5x**
appeals!" (seen in Dave's ticker), and the balance floors silently at 0 so the
number in the message is a lie as well as ungrammatical.
**Where:** `backend/admin_interface.py:1127`, `backend/user_interface.py:315`
(`award_appeals`, `max(0, ...)`).

---

## Notes that are not mine to file

- **`admin_get_shot` 500s on some shots** and takes the admin page with it:
  `TypeError: color must be int or single-element tuple` in
  `backend/image_processing.py:254` (`draw_cross_on_image`, greyscale image).
  Full traceback at `$S/backend.log:1847690`. The uncaught rejection also raises
  a CRA dev-server error overlay that intercepts every click on `/admin/shots`
  until dismissed (`a6_38_devserver_overlay.png`). The shot it choked on belongs
  to **A12**, not to me, so I have not chased it — flagging it for whoever owns
  the shot queue.
- `/admin/shots` overflows horizontally by 11px at 390px wide
  (`scrollWidth 401` vs `innerWidth 390`), which is enough to clip the left edge
  of the shot photo and the appeal panel once the page has scrolled
  (`a6_13_admin_queue_contested.png`). Cosmetic, whole-page, not appeals-specific.
- The player's "My shots" list is sorted on `time_created`, which SQLite stores
  at **1-second** resolution, so shots fired in the same second come out in an
  arbitrary order (mine listed A5, A6, A7, A1, A2, A3, A4). A real game's shot
  timeout makes this unlikely for one shooter's own shots, but the list merges
  fired and received shots, which can genuinely tie. Artefact of my rapid-fire
  fixture as much as a bug; noting it because appeals are found by browsing
  this list.
