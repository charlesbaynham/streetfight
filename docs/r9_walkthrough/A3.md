# A3 — loot lifecycle

Game `aebeb7bf-717d-40fb-8dd9-24638791568e` ("A3-Red" / "A3-Blue"), players
`A3-Alice`, `A3-Alice2`, `A3-Bob`, `A3-Dave`, `A3-Carol`, `A3-Eve`, `A3-Frank`,
`A3-Probe*`, `A3-Scanner`. No repo file was touched. No vision model was
involved in any of this — items never go near CharlesBot.

Driver scripts: `$S/a3/*.js`; raw results `$S/a3/run{2,3,4}.json`,
`$S/a3/probe{,2,3,4}.log`, `$S/a3/qrscan.log`.

## Scanning a loot QR code to pick up an item, and using it (weapons, armour, ammo, medpacks).

**Verdict:** WORKS WITH CAVEATS

**Evidence:**

- `$S/shots/a3_40_camera_sees_qr.png` — the *real* scanner path, not a URL
  navigation: I rendered the admin page's own item QR (`NewItems.js`
  `react-qr-code`), converted it to a Y4M and fed it to Chromium as the fake
  camera (`$S/a3/qrscan.js`). The screenshot shows the QR filling the camera
  view; `QRParser` read it unaided and the HUD already reads **Ammo x9** with
  the ticker line "A3-Scanner collected 9x ammo". The in-game camera pickup
  works end to end.
- `$S/shots/a3_11_ammo_after.png` — Ammo `x5` + bullet icon, ticker
  "A3-Alice2 collected 5x ammo".
- `$S/shots/a3_12_armour_after.png` — two helmet icons in the Armour slot
  (hp 1 → 3), ticker "A3-Alice2 collected a level 2 armour".
- `$S/shots/a3_13_omg_after.png` — the purple OMG rifle in the weapon slot
  (shot_damage 3), ticker "A3-Alice2 collected a OMG".
- `$S/shots/a3_24_bob_knocked_out.png`, `a3_26_bob_after_medpack.png` — medpack
  revives a knocked-out player (hp 0 → 1, "A3-Bob was revived with a medpack!").
- `$S/shots/a3_21_bob_team_ammo.png` — a `collected_as_team` ammo item scanned
  by Bob gave Bob *and* Dave +3; ticker "A3-Bob collected 3x ammo for everyone
  in their team". Dave re-scanning it → 403; Carol (other team) → +3.

The HUD itself is legible at 390×844: dark 50 %-black panel, large icons,
ammo collapses to `icon xN` above 3. Rules all hold on the backend
(`$S/a3/run4.json`):

| case | result |
| --- | --- |
| single-use item, same player twice | 403 "Item has already been collected" |
| single-use item, second player | 403 same |
| `collected_only_once=false`, same player twice | 403 (correct: per-user) |
| `collected_only_once=false`, other player | 200 |
| team item, second team-mate | 403 |
| armour no better than current | 403 "You already have armour as good as this" |
| same weapon twice | 403 "You have already got this weapon" |
| medpack on a live player | 403 "Medpacks can only be used on knocked-out players" |
| any item while **dead** | 403 "Cannot collect, you are dead!" |
| item data missing a field at mint time | 400 with the pydantic detail |
| unsigned payload | 403 "Item not signed" |
| flipped character in the base64 | 400 "Malformed data" |

### Finding: a knocked-out player cannot collect a medpack from a link
**Severity:** major
**What happens:** `CollectItemFromQueryParam` is rendered inside
`BulletCount`, and `UserMode` only renders `BulletCount` while the player is
alive. So a knocked-out (or dead) player who opens an item URL gets nothing at
all: no request is made, the `?d=…` stays in the address bar, hp stays 0. The
knocked-out screen meanwhile says "Get a medkit quick!".
The in-game camera scanner (`QRParser`, mounted inside `WebcamView`, which
*is* rendered when dead) is unaffected — that path works.
**How to reproduce:** knock a player out (`admin_hit_user`), then navigate
their browser to a medpack `encoded_url`. Screenshot
`$S/shots/a3_25_bob_medpack_url_while_knocked_out.png`: still knocked out,
URL still carries `?d=`. The same payload posted to `collect_item` directly
returns 200 and revives them (`$S/a3/run3.json`, steps
`medpack_url_while_knocked_out` vs `medpack_api_while_knocked_out`).
**Where:** `react-ui/src/UserMode.js:74` (`isAlive ? <BulletCount…`) and
`react-ui/src/BulletCount.js:106`.
**Why it matters for the 19th:** the roadmap's dry-run plan is "QR codes go
out on WhatsApp, players follow links". A medpack delivered as a link is
exactly the item a player needs while knocked out, and it is the one case
where the link path is dead. Anyone who scans with the *phone's* camera app
rather than in-game lands on the same URL and hits the same wall.

### Finding: every failure on the link path is completely silent
**Severity:** major
**What happens:** `CollectItemFromQueryParam` logs the response to the console
and unconditionally `navigate("/")`. Player pages leave `apiErrorHandler`
unset. So "already collected", "you are dead", "medpack on a live player",
"you already have this weapon", "signature mismatch" and a 500 all look
identical to the player: the app blinks back to the game screen with nothing
changed and nothing said. The camera path at least flashes red and plays
`error.mp3` on a 403 (`QRParser.js`), so the two routes to the same item give
opposite feedback.
**How to reproduce:** `$S/shots/a3_20_bob_second_scan_of_used_item.png` —
Bob opened an already-collected ammo URL; ammo still shows ✗, no message, no
flash, no ticker line.
**Where:** `react-ui/src/CollectItemsFromQueryParams.js:29-34`;
`react-ui/src/utils.js:26-28`.
**Why it matters for the 19th:** "I scanned it and nothing happened" is the
support call, and neither the player nor the admin can tell whether the code
was already taken, the player was dead, or the QR was damaged.

### Finding: `collected_as_team` is offered for every item type but only implemented for ammo
**Severity:** major
**What happens:** the admin's New-items form shows the `collected_as_team`
checkbox for all four types. Ticking it for armour, medpack or weapon produces
a perfectly valid signed QR that can never be collected: `_ACTIONS` has no
entry for those keys, so `do_item_actions` raises `NotImplementedError` —
which, being a `RuntimeError` subclass, is caught by `collect_item` and served
to the player as
`403 Item collection for (<ItemType.ARMOUR: 'armour'>, True) has not been implemented`.
A raw Python enum repr in a player-facing string.
**How to reproduce:** `$S/a3/run3.json`, steps `armour_collected_as_team` and
`medpack_collected_as_team`.
**Where:** `backend/item_actions.py:134-148`; `backend/user_interface.py:740`;
the checkbox at `react-ui/src/NewItems.js:132`.
**Why it matters for the 19th:** drops are minted from this form and printed.
A team medpack or team armour is an obvious thing to want; nothing warns you
it is not a thing until a player is standing in front of the poster.

### Finding: a weapon whose damage/timeout pair is not in the lookup shows as nothing at all
**Severity:** minor
**What happens:** I minted `{"shot_damage": 7, "shot_timeout": 2}`. It is
collected happily (the player really does end up with damage 7 / timeout 2),
the ticker announces "A3-Alice2 collected a **&lt;unnamed&gt;**" — the literal
placeholder, angle brackets and all — and the HUD weapon slot goes **blank**,
because `getGunImgFromUser` returns `null` for any pair it doesn't recognise
and the slot renders an `<img>` with no `src`. Visually indistinguishable from
having no weapon at all.
**How to reproduce:** `$S/shots/a3_14_bogus_after.png` (compare
`a3_13_omg_after.png`, same player one item earlier).
**Where:** `backend/item_actions.py:124`; `react-ui/src/utils.js:89-99`.
**Why it matters for the 19th:** the mint form takes two free-number fields
with no validation against `WEAPON_NAME_LOOKUP`. One typo when generating the
drops and a player carries an invisible gun all night.

### Finding: the "new item" pickup overlay does not actually appear
**Severity:** minor
**What happens:** `TemporaryOverlay` is meant to throw the item's icon
full-screen for about a second. Sampled every animation frame from the first
frame of the document (`$S/a3/probe3.js`), its computed `display` stayed
`none` and its box stayed `0×0` for the whole event while `opacity` animated
0 → 1 → 0 over ~3 s. So the state machine runs, the beep and the vibrate fire,
but nothing is painted. A separate slower probe caught one frame where it
*was* `display:block` at opacity 0.66 — rendered tiny in the top-left corner
over the HUD, not centred (`$S/shots/a3_probe_overlay_1.png`). A 22-frame
screenshot burst across a pickup (`$S/shots/a3_burst_00..21.png`) shows no
overlay at any point.
**How to reproduce:** `node $S/a3/probe3.js`; also `probe4.js` for the burst.
**Where:** `react-ui/src/TemporaryOverlay.js:41-72` — `display` lives inside
the framer variants (`visible.display: "block"` / `hidden.transitionEnd.display:
"none"`) alongside `width`/`height` keyframe arrays.
**Why it matters for the 19th:** it is the only *visual* confirmation of a
pickup on the player's own screen; the HUD counter and the ticker still update,
so this is polish rather than a blocker. Caveat: measured in headless
Chromium. The opacity animation ran normally, so framer-motion itself is
working and I do not think this is a headless artefact — but it is a
ten-second check on a real phone.
**Aside (answers the brief's question):** when it *does* paint it is a
`position: fixed`, 100 %×100 %, `z-index: 10` image, so it covers the
crosshair by design for ~1 s. That is intended, not a defect.

### Finding: `collect_item` 500s on QR payloads that are not items
**Severity:** minor
**What happens:** `QRParser` posts **every** QR it decodes to `collect_item`.
Two shapes reach an unhandled exception rather than a 4xx:
a URL with no `d` parameter (`KeyError` at `backend/items.py:79`) and base64
that decodes to valid JSON that isn't an object, e.g. `MTIz` → `123`
(`TypeError` at `backend/items.py:97`). Both return
`500 Internal Server Error` with a traceback in the log.
**How to reproduce:** `$S/a3/run2.json`, steps `tamper_url_no_d` and
`tamper_number`.
**Where:** `backend/main.py:382-390` catches only `ValueError`.
**Why it matters for the 19th:** any other QR code in the street — a poster, a
menu, a bus stop — pointed at by an in-game camera produces a 500 and a
traceback, which is noise you have to read past when something real breaks.
No player-visible harm (the scan silently does nothing either way).

### Finding: the fire button says "NO AMMO!" to a player who has ammo but no weapon
**Severity:** minor
**What happens:** `userCanShoot = isInTeam && hasBullets && hasWeapon`, and the
single "can't shoot" artwork is `firebutton_no_ammo.svg`. Players start with
`DEFAULT_SHOT_DAMAGE = 0`, so the very first thing that happens after a
successful ammo pickup is a HUD reading "Ammo x8" next to a button reading
"NO AMMO!".
**How to reproduce:** `$S/shots/a3_burst_06.png` (ammo x8, "No weapon",
button says NO AMMO!) vs `$S/shots/a3_13_omg_after.png` (ammo + gun, plain
button).
**Where:** `react-ui/src/FireButton.js:14-17`.
**Why it matters for the 19th:** it sends people hunting for ammo they already
have instead of for a gun, at the exact moment they are learning the game.

### Finding: ticker text is white-on-white when the camera is pointed at anything bright
**Severity:** cosmetic
**What happens:** the ticker has no background panel — white text with a 2 px
black shadow straight over the camera feed. The HUD panel next to it has
`rgba(0,0,0,0.5)` behind it and is fine.
**How to reproduce:** `$S/shots/a3_40_camera_sees_qr.png` — the camera is
looking at a printed white QR poster and the three ticker lines are barely
readable. That is the normal posture for collecting loot.
**Where:** `react-ui/src/TickerView.module.css:1-5`.

### Finding: on the knocked-out screen the headline is the smallest text
**Severity:** cosmetic
**What happens:** "You are knocked out" renders smaller than the two
`.textSmaller` lines beneath it. `index.css` sets `* { font-size: 12px }`,
which beats inheritance for the unclassed `<p>`, while the two lines below
carry `.textSmaller` (`large`, 18 px) and win. The intended hierarchy is
inverted; the heavy 0.1em drop shadow (from `.centeringDiv`, computed at the
`xx-large` size) makes it look doubled.
**How to reproduce:** `$S/shots/a3_24_bob_knocked_out.png`.
**Where:** `react-ui/src/GuideImages.module.css:1-13` vs
`react-ui/src/index.css:16-20`.

### Question: `WEBSITE_URL` has no port in this container
**Severity:** question
`admin_make_new_item` returned `http://localhost?d=…` (from `.env`
`WEBSITE_URL="http://localhost"`). I rewrote the host to `127.0.0.1:3000` to
test. That is a container-config artefact, not a repo bug — but it is the same
variable that gets baked into the printed QR codes, so it is worth confirming
it is right on the droplet before the print run. Note also that
`add_params_to_url` produced `http://localhost?d=…` with no `/` path
separator; browsers normalise it, so it works.

### Not tested here
- Reading a QR off a real printed poster in real light (I fed a synthetic,
  perfectly-lit, screen-sized code to a fake camera). The dense v10-ish code
  the long signed payload produces decoded fine at ~180 px in a 640×480 frame,
  which is encouraging but is not a field test.
- Sound and vibration on pickup (no audio device here).
