from setuptools import find_packages
from setuptools import setup

setup(
    name="backend",
    version="0.1",
    package_dir={"backend": "."},
    install_requires=[],
    author="Charles Baynham",
)
